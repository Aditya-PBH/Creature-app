// Firebase Config - Apni Firebase project ki details yahan daalo
// https://console.firebase.google.com pe jao → New Project → Web App

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

export default firebaseConfig;

// Firebase setup karne ke baad:
// npm install firebase
// Phir is config ko import karo jahan chahiye
